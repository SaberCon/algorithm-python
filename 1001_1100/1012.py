class Solution:
    def numDupDigitsAtMostN(self, n: int) -> int:
        nums = [int(c) for c in reversed(str(n))]
        ans = 0
        for i, num in nums:
            ans +=
        return n - ans
